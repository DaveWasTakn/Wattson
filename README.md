# OCPP Server (WIP!!)

Java-based implementation of an Open Charge Point Protocol (OCPP) 1.6 compliant server.
Also, manually implements the necessary WebSocket specification (i.e., parsing and sending WebSocket frames). 

